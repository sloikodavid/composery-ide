text/*; code --reuse-window %s
application/json; code --reuse-window %s
application/xml; code --reuse-window %s
application/x-yaml; code --reuse-window %s
application/yaml; code --reuse-window %s
application/toml; code --reuse-window %s
application/x-toml; code --reuse-window %s
application/sql; code --reuse-window %s
application/x-shellscript; code --reuse-window %s
