# Composery gates Docker on one decision, shared with the supervisor profile.
# ExecCondition is the systemd primitive for exactly this: a non-zero exit skips
# the unit and leaves it inactive, rather than marking it failed and retrying.
[Service]
EnvironmentFile=-/run/composery.env
ExecCondition=/opt/composery/docker.sh check
