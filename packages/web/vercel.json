{
	"$schema": "https://openapi.vercel.sh/vercel.json",
	"buildCommand": "pnpm env:deploy && npx convex deploy --cmd 'pnpm build' --cmd-url-env-var-name NEXT_PUBLIC_CONVEX_URL",
	"framework": "nextjs",
	"git": {
		"deploymentEnabled": {
			"*": false,
			"main": true
		}
	},
	"github": {
		"silent": true
	},
	"installCommand": "cd ../.. && pnpm install"
}
